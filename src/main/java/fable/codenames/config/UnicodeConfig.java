package fable.codenames.config;

public class UnicodeConfig {

    // false = unicode OFF globally
    // true  = unicode ON globally
    public static final boolean UNICODE_GLOBAL = false;

    private UnicodeConfig() {

    }
}